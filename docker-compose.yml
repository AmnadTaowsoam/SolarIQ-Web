services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: solariq-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:3000"
    env_file:
      - .env.local
